namespace Tests.SaveSystems
{
    public enum FileElements
    {
        fileStart,
        version,
        dataOpener,
        serializerID,
        serializerVersion,
        serializerValue,
        dataEnd,
        fileEnd
    }
}

